1- Please make sure you have Docker installed on your machine
2- After cloning the repo, cd to "Dockerized-Django" and run "docker-compose build -d --up"
3- Wait untill the containers are created
4- Run the executable file 
5- Press on scan USB then insert your USB stick
6- You should be able to use the software from here on